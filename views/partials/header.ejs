<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scatch</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['helvetica_now_display']">
    <nav class="w-full flex items-center justify-between px-4 py-2 border-b">
        <h3 class="text-lg font-semibold">Scatch</h3>
        
        <% var loggedin = typeof loggedin !== "undefined" ? loggedin : true %>
        <% if (loggedin) { %>
            <div class="hidden md:flex space-x-4">
                <a href="/shop" class="hover:text-gray-700">Shop</a>
                <a href="/cart" class="hover:text-gray-700">Cart</a>
                <a href="/users/profile" class="hover:text-gray-700">My Account</a>
                <a href="/owners/admin" class="hover:text-gray-700" >Create product</a>
                <a class="text-red-500 hover:text-red-600" href="/users/logout">Logout</a>
                
            </div>
        <% } %>
        <div class="md:hidden">
            <i class="ri-menu-line text-xl"></i>
        </div>
    </nav>
</body>

</html>

   